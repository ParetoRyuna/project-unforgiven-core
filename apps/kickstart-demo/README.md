# Kickstart Demo (UNFORGIVEN v2)

This folder is reserved for the EasyA demo app that integrates the Universal Shield SDK.

## Rules
- Demo code here is `v2-only`.
- Do not import from `programs/unforgiven` directly.
- Prefer importing through `packages/universal-shield-sdk`.
